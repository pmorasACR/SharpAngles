namespace MyAPI.Models
{
    /// <summary>
    /// Represents a single server that a codebase can be deployed to.
    /// Contains information needed to access it, including IP and Host name
    /// </summary>
    internal class DeploymentServer
    {
        public long Id {get;set;} 
    }
}